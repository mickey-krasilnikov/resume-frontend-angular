import { ExpirationPipe } from './expiration.pipe';

describe('ExpirationPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpirationPipe();
    expect(pipe).toBeTruthy();
  });
});
